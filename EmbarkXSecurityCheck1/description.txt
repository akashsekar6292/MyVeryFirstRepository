managing multiple user in in-memory Authentication

here we create a bean(@User) for UserDetailService to have multiple users.
For each users we set separate username and password.

we can able to give the details of the user for authentication.

If we give the username and password that we stated in application.properties, the request get unauthorized.